/*
 * Post the 1.1 release cleanup and requested immediate features patches
 */

INSERT INTO auth(name, page, display, sortOrder)
VALUES ('lookup', 'Y', 'Reg Lookup', 65);

INSERT INTO patchLog(id, name) VALUES(xx, 'Post 1.1 Release Cleanup');
